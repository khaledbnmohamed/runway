---
# See full syntax at https://docs.onica.com/projects/runway
deployments:
  - name: example
    modules:
      - path: sampleapp.cfn
    regions:
      - us-east-1

variables:
  parameters:
    dev:
      namespace: example-dev
      regions:
        - us-east-1
