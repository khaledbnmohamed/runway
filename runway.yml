---
# See full syntax at https://docs.onica.com/projects/runway
deployments:
  - modules:
      - path: sampleapp.cfn
      # - etc...
    regions:
      - us-east-1
  - ignore_git_branch: true


variables:
  regions:
    dev:
      - us-east-1
